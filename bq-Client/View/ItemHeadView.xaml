<UserControl x:Class="bq_Client.View.ItemHeadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="0,0,5,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" MaxWidth="25" MinWidth="15" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" MaxWidth="25" MinWidth="15" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>        
        <TextBlock Grid.Column="2" Text="{Binding GroupHeader}" FontSize="20"/>        
        <TextBlock Grid.Column="4" Text="{Binding Title}" FontSize="20"/>
    </Grid>
</UserControl>
